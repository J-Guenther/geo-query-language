export {tokenize} from './tokenize'
export {parse} from './parse'

// TODO add token_type Enum and use it
// TODO put tests in subfolder next to src
// TODO parser needs to handle functions
// TODO parser needs to handle parenthesis (subgroups)
// TODO parser and tokenizer needs to handle list of columns instead of only "*"
// TODO add Runner
// TODO make Layer an array like datastructur, so Runner can use .filter(expression) on it
