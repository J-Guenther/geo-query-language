{
  "name": "geo-query-language",
  "version": "0.0.1",
  "description": "A query language for vector geo data",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "test": "mocha --reporter spec --require ts-node/register 'tests/**/*.test.ts'",
    "coverage": "nyc yarn test"
  },
  "devDependencies": {
    "@types/chai": "^4.3.4",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.7",
    "mocha": "^10.1.0",
    "nyc": "^15.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
